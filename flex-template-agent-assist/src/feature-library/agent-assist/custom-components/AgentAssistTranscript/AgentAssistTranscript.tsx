// @ts-nocheck

import { Box } from '@twilio-paste/core/box';

import { getEnabledFeatureList } from '../../config';

export const AgentAssistTranscript = () => {
  return (
    <Box width={'100%'}>
      <agent-assist-transcript />
    </Box>
  );
};
